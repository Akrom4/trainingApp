<form method="post" action="{{ path('app_courses_delete', {'id': course.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ course.id) }}">
    <button class="btn btn-danger mt-2 mb-2">Supprimer</button>
</form>
