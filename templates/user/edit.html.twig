{% extends 'base.html.twig' %}

{% block title %}
	Edit User
{% endblock %}

{% block body %}
	<h1 class="text-center mt-3">Utilisateur</h1>

	{{ include('user/_form.html.twig', {'button_label': 'Enregistrer'}) }}

	{% if user.id is not null %}
		{{ include('user/_delete_form.html.twig') }}
	{% endif %}
	<div class="mt-2">
		<a href="{{ path('app_user_index') }}">retour à la liste</a>
	</div>
{% endblock %}
